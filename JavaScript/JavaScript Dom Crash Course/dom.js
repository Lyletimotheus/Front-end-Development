// Examine the DOM 
// console.dir(document);  This will show us all the properties attached to the DOM

// Here we are getting the domain name
console.log(document.domain);

// We can access the URL
console.log(document.URL);

// Title of the page
console.log(document.title);

// Here we override the current title to anything we want it to be 
// document.title = 123;

// Finding out the doctype
console.log(document.doctype);

// Raw HTML of the head
console.log(document.head);

// Raw HTML of the body
console.log(document.body);

// HTML collection of everything that is in the DOM
console.log(document.all);

// Get all the forms on the page
console.log(document.forms);

// Get all the links on the page
console.log(document.links);

// Get all the images
console.log(document.images);

